<!DOCTYPE html>
<html>
<head>
  <title>Scientific Protocols - <%= yield_or_default(:title, 'A free and easy way to share scientific protocols') %></title>
  <%= favicon_link_tag 'favicon.ico' %>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <meta name=viewport content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>
</head>
<body>
  <%= render partial: 'shared/masthead' %>
  <% if notice %>
    <p class="alert alert-success"><%= notice %></p>
  <% end %>
  <% if alert %>
    <p class="alert alert-danger"><%= alert %></p>
  <% end %>
  <%= yield %>
  <div class="container">
    <%= render partial: 'shared/footer' %>
  </div>
  <%= javascript_include_tag 'application_footer' %>
  <%= render partial: 'shared/google_analytics' if Rails.env.production? %>
</body>
</html>

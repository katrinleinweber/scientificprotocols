<%= form_for(@protocol, html: { class: 'form-horizontal', role: 'form' }) do |f| %>
  <% if @protocol.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@protocol.errors.count, 'error') %> prohibited this protocol from being saved:</h2>
      <ul>
      <% @protocol.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <%= f.label :title %>
    <%= f.text_field :title, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label :description %>
    <%= f.text_area :description, class: 'form-control', 'data-provide' => 'markdown', rows: '20' %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

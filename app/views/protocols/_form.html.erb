<%= render partial: 'shared/jquery_ui' %>
<%= bootstrap_form_for(@protocol, inline_errors: false) do |f| %>
  <%= f.alert_message t('errors.messages.not_saved') %>
  <%= f.text_field :title %>
  <%= f.text_area :description, help: t('views.protocols.form.help_description_html'), 'data-provide' => 'markdown', rows: '20' %>
  <%= f.text_field :tag_list, label: 'Tags (optional)', class: 'token-input tokenize', help: t('views.protocols.form.help_tag_list') %>
  <div class="actions">
    <%= f.submit 'Save', class: 'btn btn-primary' %>
  </div>
<% end %>
